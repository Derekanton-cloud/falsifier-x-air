[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "falsifier-x-air"
version = "0.1.0"
dependencies = ["numpy>=1.26", "pandas>=2.2", "scikit-learn>=1.5", "networkx>=3.3", "timezonefinder>=8.0,<9"]

[tool.pytest.ini_options]
pythonpath = ["."]
